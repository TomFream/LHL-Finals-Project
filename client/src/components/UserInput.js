import react, { Fragment } from 'react';
import { Input, InputLabel } from 'material-ui/core';

const UserInput = (props) => {
  return (
    <Fragment>
      <InputLabel />
      <Input />
    <Fragment />
  );
}